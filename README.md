自动安装、手动设置api、选择更新配置文件方式

```bash
curl -fsSL https://raw.githubusercontent.com/Choimoe/codex-cli-installer/refs/heads/main/install.sh | bash
```

合并之前丢失的会话信息（你可以把这一步直接喂给你的ai）：

```bash
curl -fsSL https://raw.githubusercontent.com/Choimoe/codex-cli-installer/refs/heads/main/merge.sh | bash
```
